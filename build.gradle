plugins {
    id 'base'
}

tasks.register('backend') {
    dependsOn(':app:backend:bootRun')
}

tasks.register('frontend') {
    dependsOn(':app:frontend:serve')
}

tasks.register('frontend-dev') {
    dependsOn(':app:frontend:runDev')
}

